apiVersion: v1
kind: Service
metadata:
  name: {{ template "template-kotlin-service.name" . }}
  labels:
    app: {{ template "template-kotlin-service.name" . }}
    chart: {{ template "template-kotlin-service.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "template-kotlin-service.name" . }}
    release: {{ .Release.Name }}
